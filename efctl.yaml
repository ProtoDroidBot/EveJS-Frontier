# EveJS-managed efctl workspace configuration. The repository root contains
# the two pinned submodules under the directory names efctl expects.
with-frontend: true
with-graphql: true

world-contracts-url: "https://github.com/ProtoDroidBot/world-contracts.git"
world-contracts-ref: "world-stuff-001"

builder-scaffold-url: "https://github.com/ProtoDroidBot/builder-scaffold.git"
builder-scaffold-ref: "main"

git-autocrlf: false
container-engine: auto-detect
host: "127.0.0.1"
expose-postgres: false
